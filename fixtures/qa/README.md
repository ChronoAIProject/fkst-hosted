# Local QA Fixtures

This directory owns future synthetic, non-secret Local QA Runtime fixtures. It
currently contains no executable project, guest image, Secret material, test
vector, artifact, or normative runtime example. Each fixture needs a dedicated
issue and must remain inert during installation and validation.
