---
---

Make the `Require linked issue` and `Require changeset` PR gate workflows resilient to transient GitHub API failures (bounded retries; body-grep signal first for the linked-issue gate). No package changes.
