---
"fkst-hosted": minor
---

Make the object/ownership authorization layer database-free and verify the NyxID GitHub-connections binding (issue #142). Goal ownership is already sourced from the in-memory `GoalIssueStore` (the `GoalDoc.owner_user_id`, a NyxID `sub`, mirrored to the goal issue's server-controlled hidden marker but never trusted from the editable body) and session ownership from the session's owner `sub`, with org-scoped list visibility filtered in memory — so no Mongo read remains on the ownership/visibility path and the owner comparison is `sub == sub`. Because #137 records the owner as the NyxID subject (not a GitHub login), **no subject↔GitHub-login mapping is needed** for authorization; the spec's `github_logins_for` resolver and per-read marker parsing are obviated. The pure `allows` policy and the action layer (`require_permission`, `fkst:*`) are unchanged; anti-enumeration (Read denial → `NotFound`) and fail-closed (NyxID outage on a needed org-role lookup → `Unavailable`) are preserved and pinned by named tests. The `UNVERIFIED` flag on `GITHUB_CONNECTIONS_PATH` is resolved: verified against NyxID `main` @ `bcaccc9` (v0.7.0) the assumed `{connection_id, login, primary}` connections shape does **not** exist (the real per-account listing is `GET /api/v1/providers/my-tokens`); that binding is consumed only by the github-issues hub (not authorization), so the note now records the verified finding and the corrective work is tracked in #156.
