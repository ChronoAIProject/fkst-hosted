---
"fkst-hosted": minor
---

Add the Job watcher: maps a session Job's terminal status onto its goal issue
(lifecycle labels + a final summary comment via the App token) and drives the
NyxID Path-B token refresh while the session runs (milestone #9).
