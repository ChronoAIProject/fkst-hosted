---
---

Add the Rust CI workflow (no-op until `backend/` exists) and the Cargo version-mirror script wiring to `sync-release-pr.yml`, keeping `develop` byte-identical with `develop-auto` for CI parity. No package changes.
