---
"fkst-hosted": patch
---

Add the worker engine executor (#171, increment 4 of #151): `fkst-worker`'s `execute_dispatch` clones the goal repo, sets up CODEX_HOME (rendered `config.toml` + ornn skills + AGENTS.md), builds the `GoalContext` + `StartSpec`, and spawns the engine via `SessionRunner::start_with_spec`, returning an `ExecutedSession` that owns the clone + codex guards for the session lifetime — mirroring the control-plane `drive_inner` start path exactly, but worker-side. The ornn on-disk install machinery (`install_skill`, the AGENTS.md marker-block, the path-traversal/exec-bit guards) moves into the worker-reachable `fkst-engine` crate so the in-process driver and the worker share one implementation (control-plane's `ornn::inject` becomes a thin, behaviour-identical adapter). The executor is wired into the worker agent's `ResolvedDispatch` control arm and registers the session for `Heartbeat.running_sessions`, but the controller does not emit `ResolvedDispatch` yet, so it is dormant and behaviour is unchanged. A `Cloner` trait seam keeps the production path a verbatim `clone_repo_packages` pass-through while making the executor offline-testable; no secret is logged.
