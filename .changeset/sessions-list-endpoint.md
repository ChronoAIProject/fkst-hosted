---
"fkst-hosted": minor
---

Add `GET /api/v1/sessions` to list the caller's sessions (owner + visible orgs + legacy; `fkst:admin` sees all), newest-first, with a `status` filter and `limit`/`offset` pagination.
