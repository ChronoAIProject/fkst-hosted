---
"fkst-hosted": minor
---

Add the Kubernetes client (`kube-rs`) and the pod-per-session dispatch
configuration (`FKST_POD_*`, off by default) plus RBAC sample manifests, the
foundation for spawning one Job per substrate session (milestone #9).
