---
---

Docker build CI: fall back to the committed `backend/engine.ref` pin when the optional `FKST_SUBSTRATE_REF` Actions variable is unset (the variable remains a drift assertion when present). No package changes.
