---
"fkst-hosted": minor
---

Add a GitHub issues hub: aggregate a user's GitHub issues across all their linked accounts (resilient to per-account failure) and create/read/update/comment on issues, all through NyxID's credential-injection proxy (RFC 8693 delegation) — fkst-hosted never holds GitHub tokens.
