---
"fkst-hosted": minor
---

Add the Changesets + SemVer release pipeline: a per-PR changeset gate on `develop`, a per-release release-note gate on `develop → main`, automated version computation, tagging, GitHub Releases, and root `CHANGELOG.md` maintenance.
