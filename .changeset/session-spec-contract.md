---
"fkst-hosted": minor
---

Add the pod-per-session `SessionSpec` + credential-layout contract (the typed
control-plane→pod handoff for milestone #9) and remove the now-dead
worker-protocol wire types left after the single-crate restructure.
