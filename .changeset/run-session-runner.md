---
"fkst-hosted": minor
---

Add the `run-session` subcommand: the in-pod entrypoint that reads a SessionSpec
+ mounted credential files and drives one substrate engine session to
completion, exit code reflecting disposition (milestone #9).
