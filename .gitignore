# Rust build artifacts
backend/target/
apps/local-qa-runtime/target/
**/*.rs.bk

# Node
node_modules/
apps/local-qa-runtime/workers/dist/
packages/qa-contracts/dist/
packages/qa-contracts/dist-test/

# Secrets and credentials — never commit these
.env
.env.*
!frontend/.env.example
!backend/.env.example
*.pem
*.key
credentials.json

# Local PRODUCTION manifests (hold real cluster values/secrets) — never commit.
backend/k8s_prod/
frontend/k8s_prod/

# Local AI-agent session state (worktrees, settings) — never commit.
.claude/

# OS noise
.DS_Store

# /auto engine working state
.auto/
.gstack/
